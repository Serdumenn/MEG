name = "meg-gemini-proxy"
main = "index.js"
compatibility_date = "2025-01-01"

# Deploy secrets (never put real values in this file):
#   wrangler secret put GEMINI_API_KEY
#   wrangler secret put WORKER_TOKEN   ← generate with: openssl rand -hex 32
#
# Deploy the worker:
#   wrangler deploy
#
# Test locally (runs at http://localhost:8787):
#   wrangler dev
